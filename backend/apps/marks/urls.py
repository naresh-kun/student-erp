from django.urls import path

app_name = 'marks'

# REST endpoint routes (Planned for backend implementation phases)
urlpatterns = [
    # path('', MarkListView.as_view(), name='mark_list'),
    # path('bulk/', BulkMarkEntryView.as_view(), name='mark_bulk'),
]
