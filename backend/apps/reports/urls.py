from django.urls import path

app_name = 'reports'

# REST endpoint routes (Planned for backend implementation phases)
urlpatterns = [
    # path('attendance-summary/', AttendanceSummaryReportView.as_view(), name='attendance_summary'),
    # path('academic-performance/', PerformanceReportView.as_view(), name='academic_performance'),
]
