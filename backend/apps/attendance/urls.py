from django.urls import path

app_name = 'attendance'

# REST endpoint routes (Planned for backend implementation phases)
urlpatterns = [
    # path('', AttendanceListView.as_view(), name='attendance_list'),
    # path('bulk/', BulkAttendanceRecordView.as_view(), name='attendance_bulk'),
]
