# Reusable Components

This directory houses reusable atomic UI primitives modeled after shadcn/ui (Button, Card, Input, Modal, Dropdown, Tabs).

Domain-specific widgets belong in `features/<domain>/components/`.
